// the interface name is optional (but recommended)
export default interface Cylinder {
    radius: number;
    height: number;
    colour: string;
};

// there can be at most one default export per module